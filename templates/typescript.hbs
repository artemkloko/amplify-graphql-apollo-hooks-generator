/* tslint:disable */
/* eslint-disable */
// this is an auto generated file. This will be overwritten

import gql from "graphql-tag";
{{#each imports as |variables from| }}
import {
  {{#each variables as |variable| }}
    {{variable}},
  {{/each}}
} from "{{from}}";

{{/each}}

{{#each operationsPerType as |operationType| }}
  {{#each operations as |operation| }}
export const {{ operation.hookName }} = (
  options?: {{operationType.apolloOperation}}HookOptions<
    {{ operation.operationType }}{{#if operation.variablesType}}, {{ operation.variablesType }}{{/if}}
  >
) =>
  use{{operationType.apolloOperation}}<
    {{ operation.operationType }}{{#if operation.variablesType}}, {{ operation.variablesType }}{{/if}}
  >(
    gql({{camelCase operation.operationName}}),
    options
  );

  {{/each}}
{{/each}}

